# Adapted from WrenAI examples/v5-jaffle (Apache-2.0); modified for Adilade — see /NOTICE.
name: customers
properties:
  description: One row per customer. name may be NULL for guest checkouts.
table_reference:
  catalog: ""
  schema: public
  table: customers
columns:
  - name: id
    type: INTEGER
    is_calculated: false
    not_null: true
    is_primary_key: true
    properties: {}
  - name: name
    type: VARCHAR
    is_calculated: false
    not_null: false
    properties:
      description: Customer display name; NULL for guest checkouts.
  - name: region
    type: VARCHAR
    is_calculated: false
    not_null: true
    properties:
      description: "Sales region. One of: AMER, EMEA, APAC."
primary_key: id
cached: false
